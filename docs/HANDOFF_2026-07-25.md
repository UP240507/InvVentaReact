# Handoff — Sesión 25-jul-2026 (Fases 0 y 1 cerradas · siguiente: Fase 2)

> Avance global: **~70% → 84%**. Roadmap completo en `ROADMAP.md` (raíz).
> Tenant AZUL `15e2e574-6222-445c-afcd-c04925001aae` · proyecto Supabase `aorrfmxduefqwlrhfzzf`.
> Estrategia de lanzamiento acordada: al llegar ~95-100%, AZUL paga su plan y opera
> como sombra; en ese momento se contrata Supabase Pro (los proyectos free se
> pausan tras ~1 semana de inactividad — pre-requisito de lanzamiento, fase 4).

---

## Pre-fase: LoginScreen (rediseño + bug PIN)

- **Bug reparado:** el numpad estaba cableado a 4 dígitos; staff nuevo (PIN de 6
  obligatorio) no podía teclearlo. Ahora `PIN_MIN=4 / PIN_MAX=6` (legados 4-5
  tolerados), auto-submit al 6º dígito. Navega igual a `/checador?pin=`.
- **Re-skin editorial `adm-*`:** rail navy con lockup de marca + área crema con
  las 2 cards. Reacciona a los 3 temas × claro/oscuro por tokens.
- **Assets nuevos:** `public/brand/isotipo-tight.png` (473×653) y
  `logotipo-tight.png` (755×147) — los originales traían lienzo 1536×1024 casi
  todo transparente (por eso el logo salía chico). Originales intactos.
- El link "¿Olvidaste tu contraseña?" se sustituyó por "Crear restaurante"
  (recuperación de contraseña NO existe aún — pendiente futuro).

## Fase 0 — Seguridad y deuda ✅

- 0.1 Rate-limit `login-pin`: ya estaba vivo (EF v9, tabla `login_intentos`,
  10 fallos/IP/15min→429, índices correctos). Verificado contra lo desplegado.
- 0.2 Huérfanos `@stockcentral.com`: ya no existían.
- 0.3 Fuentes @fontsource: instaladas e importadas en `main.jsx`.
- 0.4 Contraste AA: `--adm-muted` oscurecido en los 3 temas claros
  (terracota #6a645a · vino #6c6360 · fénix #6e655b, ~5.2:1 sobre crema).
- Advisors: RPCs SECURITY DEFINER verificadas (validan tenant, rechazan anon) —
  WARN aceptado por diseño. `login_intentos` sin políticas = deny-all intencional.
- **Pendiente manual Chris:** activar Leaked Password Protection
  (dashboard Supabase → Auth → Settings).

## Fase 1 — Monetización ✅ (test mode)

### Modelo de negocio (docs/PRECIOS_InvVenta.md)
Anualidad única vía **Stripe**, sin mensualidad. Dispositivos ILIMITADOS.
Único enforcement duro: **empleados activos**. Módulos premium ocultos por
plan/addon. Fundador $3,990 (10 emp, solo primeros 10 clientes) · Básico $4,990
(10) · Pro $7,990 (25 + lealtad) · Empresarial $11,990 (60 + lealtad +
multisucursal) · Add-on Lealtad $990 · CFDI $1,990 (no disponible aún).
Centavos sin IVA (`tax_behavior=exclusive`). UI dice "más IVA" (no "+ IVA").

### BD (migraciones nuevas, replicadas a supabase/migrations/)
- `20260725170733_planes_addons_extender_suscripciones_fase1.sql`: tablas
  `planes` y `addons` (+seed) · `suscripciones` legada EXTENDIDA en sitio
  (dias_gracia, addons jsonb, trial_hasta, stripe_customer_id,
  stripe_subscription_id, cancelar_al_final, updated_at+trigger, FK plan,
  CHECK estado trial/activo/moroso/suspendido/cancelado, UNIQUE restaurante_id).
  **Fix de seguridad:** la política RLS era FOR ALL (el tenant podía alargarse
  la vigencia) → ahora SELECT-only; escribe solo service role (webhook/EFs).
- `20260725192227_suscripciones_fecha_vencimiento_nullable.sql`: el trial no
  tiene vencimiento de anualidad; la NOT NULL legada rompía el registro (bug
  cazado en prueba en caliente, tenant huérfano "Doña pelos" limpiado).

### Cliente
- `src/hooks/usePlan.js`: fuente única — `derivarPlan()` (pura) + `usePlan()`.
  Vigencia: trial→trial_hasta; activo/moroso→fecha_vencimiento+dias_gracia.
  Fallback local del catálogo para cache offline viejo.
- `useAuthStore`: la suscripción se trae con `planes(id,nombre,limites)`
  embebido y estados trial/activo/moroso (la vigencia se decide en cliente).
- `App.jsx`: `SuscripcionRoute` delega en derivarPlan; `ModuloRoute` nuevo gatea
  `/clientes` (lealtad) y `/facturas` (cfdi) → `/paywall?modulo=X`.
  Ruta pública `/registro` añadida.
- `SidebarLayout`: ítems con `modulo:` se ocultan sin plan/addon.
- `EmpleadosScreen`: aviso de límite en alta (UX; la barrera real es la EF).
- `BillingScreen` (re-hecha, adm-*): plan actual + barra de uso empleados +
  catálogo desde BD + add-ons + avisos `?pago=exito|cancelado` + botón portal.
  Lee `configuracion.nombre_empresa`.
- `PaywallScreen` (re-hecha, adm-*): modo vigencia (renovar) y modo upgrade
  contextual por `?modulo=` (plan más barato que lo incluye, o add-on).
- `RegistroScreen` (nueva): alta self-service; al éxito muestra el CÓDIGO
  corto con botón copiar.
- `checkout.js` (nuevo): `iniciarCheckout`, `abrirPortal`, `cargarCatalogo`,
  `precioMXN`.
- `ConfiguracionScreen`: tab nuevo **"Mi plan"** (plan activo + catálogo solo
  lectura + CTA a /mi-plan) y tarjeta **"Código del restaurante"** con copiar
  en el tab Restaurante.
- `PerfilScreen`: tarjeta del código del restaurante (solo flag `gestion`).

### Edge Functions desplegadas
- `create-checkout` (JWT on): solo flag gestion; precios resueltos del catálogo
  server-side; reusa stripe_customer del tenant; Checkout mode=subscription.
- `stripe-webhook` (JWT off, firma verificada): ÚNICA pieza que escribe
  `suscripciones`. checkout.completed→activo con periodo real de Stripe;
  past_due/unpaid→moroso; deleted→cancelado; cancel_at_period_end→
  cancelar_al_final. Candado Fundador: al 10º activo apaga el plan en catálogo.
- `registrar-restaurante` (JWT off, v2): auth admin → restaurante+código
  (PREFIJO-XXXX, sin 0/O/1/I) → usuarios → configuracion → suscripción TRIAL
  14 días (fundador si activo, si no básico). Rollback verificado con logging
  (v1 dejaba huérfanos por deletes silenciosos). Throttle 3 registros/IP/día.
- `crear-empleado-auth` (v11): barrera server-side del límite de empleados
  (cuenta activos, excluye el propio staffId; desactivar libera cupo).
- `customer-portal` (JWT on): billingPortal.sessions → tarjeta/facturas/
  cancelación con prorrateos de Stripe.

### Stripe (TEST mode — cuenta nueva, la primera se eliminó)
- 5 prices con lookup keys `plan_*_anual` / `addon_lealtad_anual`, metadata
  plan_id/addon_id, registrados en `planes.stripe_price_id` / `addons.…`:
  fundador `price_1TxAOKQVo2lF1fheCtLKvixC` · basico `…OLQVo2lF1fhe2A7pNPt3` ·
  pro `…OMQVo2lF1fheMSAFdLw3` · empresarial `…ONQVo2lF1fheQbLGfNvQ` ·
  lealtad `…OOQVo2lF1fheVjAqMuWr`.
- Webhook endpoint + secrets (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`)
  configurados por Chris.

### Probado en caliente ✓
- Checkout end-to-end: AZUL cambió a Empresarial, webhook escribió la fila
  (vence 24-jul-2027). AZUL quedó con addon `cfdi` para seguir viendo Facturas.
- Registro self-service ✓ (tras el fix de fecha_vencimiento).
- Los warnings de consola en la página de Stripe (preload `as`, CSP de fuente
  de Perplexity) son ajenos: Stripe + extensión del navegador. Ignorar.

### Pendientes fase 1 (no bloquean desarrollo)
1. Activar Customer Portal en Stripe (Settings → Billing → Customer portal).
2. Hot-tests faltantes: `/clientes` con tenant sin lealtad → paywall contextual;
   topar límite de empleados en tenant trial.
3. Al lanzar (fase 4): repetir prices/webhook/secrets en LIVE, decidir Stripe
   Tax vs IVA por fuera, Supabase Pro.
4. Upgrades de plan crean suscripción NUEVA en Stripe (la vieja queda viva ahí;
   la BD queda bien). Mitigación actual: Customer Portal. Mejora futura:
   cancelar la vieja en el webhook.

---

## SIGUIENTE SESIÓN: Fase 2 — Rediseño (84% → 92%)

Plan aprobado (Proyecto D, ver contexto del proyecto): híbrido editorial
(admin) / industrial (operación), tokens `adm-*` ya vivos, login ✅, tanda 1 ✅.

1. **Tanda 2 restante:** sidebar colapsable 208↔56 (`Ctrl+B`), topbar con
   búsqueda global, re-skin del interior de pantallas (StatusBar ya existe).
2. **Tanda 3 — atajos:** hook `useAtajos(scope, mapa)` sin libs + registro
   central; `Ctrl+K` palette filtrada por usePermisos; `Ctrl+1..9`, `F1`,
   `Ctrl+Shift+L`; por módulo (Mesas flechas/Enter/C/R · POS `/` F9 +/- ·
   KDS 1..n · tablas flechas/Enter/N) con hints visibles.
3. **Tanda 4 — pilotos:** Dashboard (editorial: métricas por periodo, P&L,
   alertas AUDITORIA_SISTEMA) + Mesas (industrial + inspector contextual).
   Validar el híbrido antes de escalar.
4. **Tanda 5 — resto por grupos:** catálogos/tablas (DataTable densa, zebra) →
   compras/almacén → RH/CRM → config. Operación NO cambia de paleta.
5. **Tanda 6 — pulido:** estados vacíos, transiciones 250ms, AA final, title
   bar de Tauri. (El AA de muted ya quedó en fase 0.)

Notas: ConfiguracionScreen y las pantallas nuevas de suscripciones ya usan
adm-* solo en Billing/Paywall/Registro/Login; el resto sigue en el skin viejo
hasta su tanda. El mock de Figma Make se TRADUCE, no se importa.
